# Server-side only (never commit real values).
# Used by /api/recipes and /api/adapt on Vercel, and by Vite local middleware.

LLM_PROVIDER=gemini
GEMINI_API_KEY=
# Optional aliases also supported:
# GOOGLE_API_KEY=
# LLM_API_KEY=

# Optional overrides
# LLM_MODEL=gemini-3.6-flash
# LLM_API_BASE_URL=
